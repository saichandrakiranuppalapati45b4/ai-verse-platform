// Placeholder for Gallery Manager
const GalleryManager = () => {
    return (
        <div>
            <h1>Gallery Manager</h1>
            <p className="text-muted">Upload and manage gallery images/videos</p>
            <div className="card" style={{ marginTop: 'var(--spacing-xl)' }}>
                <p>Upload images/videos, tag by event, approve/hide media, and manage gallery content.</p>
            </div>
        </div>
    );
};

export default GalleryManager;
